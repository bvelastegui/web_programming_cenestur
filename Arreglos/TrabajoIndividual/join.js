const notas = [
  10,
  8.90,
  9.66,
  6.5,
  9.51,
  7.8,
  8,
]

// La función join crea y retorna un nuevo string que concatena(une) todos los
// elementos de nuestro arreglo separado por comas
// o por el separador que nosotros le pasamos, en este caso |
console.log(notas.join(" | "))